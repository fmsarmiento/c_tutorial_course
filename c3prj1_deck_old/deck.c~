#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include "deck.h"
void print_hand(deck_t * hand){
  card ** ptr = hand->cards; //ptr is an address that points to the address of the cards
  for(int i=0;i<(hand->n_cards); i++) {
    printf(" ");
    print_card(**ptr);
  }
}

int deck_contains(deck_t * d, card_t c) {
  
  return 0;
}

void shuffle(deck_t * d){

}

void assert_full_deck(deck_t * d) {

}
